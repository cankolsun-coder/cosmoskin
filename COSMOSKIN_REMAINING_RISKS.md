# COSMOSKIN Remaining Risks

Generated: 2026-06-16T11:44:51+00:00

Only external or operational verification that could not be truthfully completed in this local environment is listed below. Locally fixed issues are not repeated.

| Risk | Why it remains | Exact action / command | Success criteria | Blocks first real order? |
|---|---|---|---|---|
| Supabase staging migration application | No connected staging database credentials were available. | Apply pending migrations in the order documented in `COSMOSKIN_PRODUCTION_DEPLOYMENT_CHECKLIST.md`, then run `supabase/verification/20260616_prelaunch_verification.sql`. | All functions/constraints/policies exist and verification queries show no critical inconsistencies. | **Yes** |
| Real PostgreSQL stock-one concurrency | Local test is deterministic mock verification, not PostgreSQL row-lock proof. | `ALLOW_DESTRUCTIVE_TEST=true TEST_INVENTORY_SLUG=qa-stock-one-product node scripts/test-inventory-concurrency.mjs` against isolated staging. | Two simultaneous qty-1 reservations against stock 1 yield exactly one success, no negative stock. | **Yes** |
| Real inventory reconciliation/seed | Production/staging catalog records were not accessible. | `node scripts/reconcile-inventory.mjs --check` with staging service-role variables; review before any seed mode. | Every active sellable slug maps once; no negative/reserved-over-stock/backorder mismatch rows. | **Yes** |
| iyzico sandbox initialization and callbacks | No sandbox API key/secret or external transaction was supplied. | Configure sandbox shell/Cloudflare secrets; run a hosted payment and replay success/failure/duplicate/invalid fixtures with `scripts/replay-iyzico-callback.mjs`. | Order/payment/token/amount/currency/basket match; success converts once; failure releases once; duplicate is no-op. | **Yes** |
| Active production bank account | Real bank data must not be embedded in source and was unavailable. | Add a protected `payment_bank_accounts` record; verify `public.is_valid_tr_iban`, `/api/payment/bank-accounts`, checkout and stored order snapshot. | `configured:true`; same normalized IBAN at checkout/confirmation; disabling account blocks order before reservation. | **Yes** for EFT launch |
| Cloudflare Access/MFA | Account-level Cloudflare configuration is external to the repository. | Create Access applications for `/admin/*` and `/api/admin/*`, require MFA and approved identities; verify anonymous denial. | Edge denies unauthenticated requests before Functions; authorized admin session works. | **Yes** |
| Protected EFT expiry cron | Scheduler deployment and secret cannot be executed locally. | Configure `CRON_SECRET`, schedule the expiry endpoint, create expired unpaid and paid controls. | Unpaid expired reservation releases once; paid order is unchanged; duplicate run is idempotent. | **Yes** if EFT is enabled |
| Real Supabase auth/reviews/tracking round trips | Browser QA used deterministic fixtures. | Run register-confirm-login-reset/session-expiry/address/order; review upload/moderation; tracking correct/wrong-email scenarios in staging. | Professional errors, ownership/privacy and DB state all match UI. | Auth/reviews: No; tracking/account at launch: recommended gate |
| Legal and skincare copy review | Technical consistency is fixed, but substantive legal/medical wording requires professionals. | Obtain Turkish e-commerce/privacy counsel review and qualified skincare/dermatology wording review. | Approved dated document versions are deployed and recorded with orders. | Legal review: **Yes** |
| Physical-device and production performance verification | Chromium responsive automation is not a physical iOS/Android or CDN test. | Test 360/390/430 widths on Safari iOS and Chrome Android; run production Lighthouse/Web Vitals. | No input zoom/clipping, CTA overlap or keyboard trap; acceptable LCP/CLS/INP. | Recommended before paid traffic |
