
import { getUserFromAccessToken, insertRow, selectRows } from './_lib/supabase.js';
import { json } from './_lib/response.js';
function tokenFromReq(req){return (req.headers.get('authorization')||'').replace(/^Bearer\s+/i,'');}
export async function onRequestGet(context){try{const token=tokenFromReq(context.request); const user=token?await getUserFromAccessToken(context,token):null; if(!user) return json({ok:false,error:'Oturum gerekli.'},{status:401}); const rows=await selectRows(context,'return_requests',{select:'*',user_id:`eq.${user.id}`,order:'created_at.desc'}); return json({ok:true,returns:rows||[]});}catch(error){return json({ok:false,error:error.message||'İade talepleri alınamadı.'},{status:500});}}
export async function onRequestPost(context){try{const body=await context.request.json().catch(()=>({})); const token=body.accessToken||tokenFromReq(context.request); const user=token?await getUserFromAccessToken(context,token):null; if(!user) return json({ok:false,error:'İade talebi için giriş gerekli.'},{status:401}); if(!body.order_id||!body.reason) return json({ok:false,error:'Sipariş ve iade nedeni gerekli.'},{status:400}); const row=await insertRow(context,'return_requests',{order_id:body.order_id,user_id:user.id,customer_email:user.email,reason:String(body.reason).slice(0,120),requested_items:Array.isArray(body.items)?body.items:[],note:String(body.note||'').slice(0,500)}); return json({ok:true,returnRequest:row});}catch(error){return json({ok:false,error:error.message||'İade talebi oluşturulamadı.'},{status:500});}}
