# COSMOSKIN Director Update — Hero + Smart Routine + Routine Commerce

## Updated
- Homepage hero now uses `assets/img/hero/hero-bg-cosmoskin-stone.png` from the provided image.
- Hero copy now keeps `Cildin. Işıltın.` on one line and `Senin hikayen.` on the next single line.
- Homepage hero editor-card and product cutouts were removed from the markup.
- Smart Routine actions were aligned and polished.
- `Rutini Gör` now uses a black background for proper contrast.
- `Rutini Kaydet` alignment and heart spacing were refined.
- Routine recommendations page was rebuilt into a commerce-focused two-column layout:
  - left: routine packages with only `Seti Sepete Ekle` CTA
  - right: sticky prepared basket carried from the homepage smart routine selection
- Routine cart products are still deduplicated before add-to-cart.

## QA
- JS syntax checked for `assets/routine-experience.js` and `assets/js/smart-routine.js`.
- HTML rough div-balance checked for `index.html` and `collections/routine.html`.
- Removed all `Rutini İncele` CTAs from the updated routine commerce flow.
